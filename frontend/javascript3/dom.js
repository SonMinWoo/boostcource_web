/css selector
//getElementByld(id): id정보를 통해 관련 정보 검색
//tree의 root: document
//document.getElementByld("id").innerText = "xxx"와 같이 값을 바꿔줄 수 있다.
//document.querySelector("div") -> 첫번째 div찾음.  id는 앞에 #붙여야함. class는 .을 붙임 바로 아래 자식은 >로 연결한다.

querySelectorAll
